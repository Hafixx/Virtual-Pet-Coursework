{
  "name": "sample-virtual-pet",
  "version": "1.0.0",
  "description": "My first server",
  "main": "server.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1", 
    "start": "node server.mjs --watch"
  },
  "author": "up2125745",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.34.0",
    "eslint-config-portsoc": "^1.0.1"
  },
"eslintConfig": {
  "extends": "portsoc",
  "root": true,
  "env": {
    "browser": true
  }
}
}

